// layouts/go-dev.kdl
layout {
    pane split_direction="vertical" {
        // Left side: Editor
        pane size="60%" {
            command "nvim"
            args "."
        }
        // Right side: Split horizontally
        pane split_direction="horizontal" {
            // Top right: Build/Test Runner
            pane {
                name "go-runner"
                command "bash"
                args "-c" "clear; echo 'Ready to run tests or build (e.g., go test ./...)'"
            }
            // Bottom right: General Commands
            pane {
                name "go-commands"
                command "bash"
            }
        }
    }
}